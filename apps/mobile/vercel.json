{
  "buildCommand": "cd ../.. && npm ci && npm run build -w @quiz-rush/shared && cd apps/mobile && npx expo export --platform web",
  "outputDirectory": "dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
